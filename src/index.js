import React from 'react';
import ReactDOM from 'react-dom/client';
import ImageFinder from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <ImageFinder />
  </React.StrictMode>,
);
